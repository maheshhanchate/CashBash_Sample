
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

@*<div>
        <div class="row"></div>
        <h5>Please Login</h5>
        <div class="form-group">
            <div class="form-group">
                <label>Username</label>
                <input type="text" class="form-control" required="required" id="uname">
            </div>
        </div>
        <div class="form-group">
            <label for="pwd">Password:</label>
            <input type="password" class="form-control" required="required" id="pwd">
        </div>
        <button type="reset" class="btn btn-primary">Clear</button>
        <button type="submit" class="btn btn-default">Submit</button>
    </div>*@
@using (Html.BeginForm("SearchUser", "Admin"))
{
    <div>
        <div class="col">
            <span class="font-weight-bold text-primary">Please Login</span>
        </div>
        <div class="col">
            <div class="form-group row justify-content-center">
                <label for="inputEmail" class="col-sm-2 col-form-label">Login ID:</label>
                <div class="col-sm-3">
                    <input type="text" class="form-control" required="required" id="uname">
                </div>
            </div>
        </div>
        <div class="col">
            <div class="form-group row justify-content-center">
                <label for="inputPassword" class="col-sm-2 col-form-label">Password:</label>
                <div class="col-sm-3">
                    <input type="password" class="form-control" id="inputPassword" required="required">
                </div>
            </div>
        </div>
        <div class="col">
            <div class="form-group row">
                <div class="col-sm-10 offset-sm-2 text-center">
                    <button type="reset" class="btn btn-primary">Clear</button>
                    <button type="submit" class="btn btn-primary" onclick="return login();">Submit</button>
                </div>
            </div>
        </div>
    </div>
}

<script>
    function login() {
        var uname = document.getElementById("uname").value;
        var upass = document.getElementById("inputPassword").value;
        if (uname == '' || upass == '')
            return true;
        if (uname == 'cashbash' && upass == 'admin') {

            return true;
        }
        else {
              alert("Wrong user id or password")

            return false;
        }
    }

</script>
